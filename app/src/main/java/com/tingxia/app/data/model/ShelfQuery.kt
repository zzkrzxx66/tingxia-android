package com.tingxia.app.data.model

enum class ShelfSort {
    RECENT,
    IMPORTED,
    TITLE,
    PROGRESS,
}

enum class ShelfFilter {
    ALL,
    NOT_STARTED,
    IN_PROGRESS,
    NEEDS_REAUTH,
}
