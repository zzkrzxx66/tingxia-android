<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the outer corners are rounded. The start edge sits underneath the artwork, so rounding it just
  cut a curved notch into the seam between cover and panel.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_surface" />
    <corners
        android:bottomLeftRadius="4dp"
        android:bottomRightRadius="@dimen/widget_corner"
        android:topLeftRadius="4dp"
        android:topRightRadius="@dimen/widget_corner" />
</shape>
